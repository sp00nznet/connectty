<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions for SSH, RDP, SFTP, and cloud provider APIs -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Biometric authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- External storage for file transfers (SFTP) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- For Android 13+ media access -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- USB Serial support for COM ports -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <uses-permission android:name="android.permission.USB_PERMISSION" />

    <application
        android:name=".ConnecttyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Connectty"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Connectty"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Support for RDP and SSH URLs -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ssh" />
                <data android:scheme="rdp" />
            </intent-filter>
        </activity>

        <!-- Terminal Activity (future implementation) -->
        <activity
            android:name=".ui.terminal.TerminalActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <!-- RDP Activity (future implementation) -->
        <activity
            android:name=".ui.rdp.RDPActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- SFTP File Browser Activity (future implementation) -->
        <activity
            android:name=".ui.sftp.SFTPActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Terminal Session Provider for Android 15+ Terminal app -->
        <!-- This allows Connectty to be set as the default shell provider -->
        <provider
            android:name=".terminal.ConnecttyTerminalProvider"
            android:authorities="com.connectty.android.terminal"
            android:exported="true"
            android:permission="com.android.terminal.permission.RUN_COMMAND">
            <intent-filter>
                <action android:name="com.android.terminal.action.PROVIDE_TERMINAL_SESSIONS" />
            </intent-filter>
            <meta-data
                android:name="com.android.terminal.provider_name"
                android:value="Connectty SSH" />
        </provider>
    </application>

</manifest>
